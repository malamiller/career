pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'packaging app'
                sh "ls"
                sh "pwd"
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying'
            }
        }
    }
}